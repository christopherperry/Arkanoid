#pragma once

enum PowerUp
{
	// Player can catch the ball and relaunch
	CATCH,

	// Turns one ball into 3 balls
	DISRUPTOR,

	// Expands the size of the paddle
	EXPAND,

	// Changes the paddle into a gunner
	GUN,

	// Shrinks the size of the paddle
	SHRINK
};

